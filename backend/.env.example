# ============================================
# DEVELOPMENT CONFIGURATION
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (REQUIRED - change this to a random string in production)
# Generate strong secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS (Frontend URL)
FRONTEND_URL=http://localhost:5173

# Admin Configuration
# Default secret path for admin access (optional - can be set during init-db)
# Your admin URL will be: /admin/{ADMIN_SECRET_PATH}
# Example: ADMIN_SECRET_PATH=my-secret-admin-2024
ADMIN_SECRET_PATH=

# Storage Configuration
# Options: 'local' or 's3'
STORAGE_TYPE=local

# AWS S3 Configuration (only needed if STORAGE_TYPE=s3)
# Get these from your AWS IAM console
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
S3_REGION=us-east-1

# Database Configuration
# Development: sqlite (default)
# Production: postgresql (use DATABASE_URL)
DB_TYPE=sqlite

# ============================================
# PRODUCTION CONFIGURATION (Railway)
# ============================================
# Set these environment variables in Railway dashboard:
#
# NODE_ENV=production
# JWT_SECRET=<generate-random-string>
# FRONTEND_URL=https://your-frontend.vercel.app
# STORAGE_TYPE=s3
# AWS_ACCESS_KEY_ID=<your-aws-key>
# AWS_SECRET_ACCESS_KEY=<your-aws-secret>
# S3_BUCKET_NAME=dolapo-portfolio-videos
# S3_REGION=us-east-1
# DATABASE_URL=<railway-provides-automatically>
#
# Note: PORT is automatically set by Railway
# Note: DATABASE_URL is automatically set when you add PostgreSQL
